# =============================================================================
# WeatherFlow — copy to .env and fill in. Loaded from project root (and backend/.env).
# =============================================================================

# ----- LLM (OpenAI-compatible) -----
# Chat + JSON use OPENAI_* unless you split embeddings below.
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_API_KEY=sk-replace-me
CHAT_MODEL=gpt-4o-mini

# Embeddings: omit both lines to reuse OPENAI_*; or set a second vendor (e.g. DashScope).
EMBEDDING_BASE_URL=
EMBEDDING_API_KEY=
EMBEDDING_MODEL=text-embedding-3-small
EMBEDDING_DIM=1536

# ----- Per-task model overrides (optional; empty = CHAT_MODEL) -----
CHAT_MODEL_STATE=
CHAT_MODEL_REFLECTION=
CHAT_MODEL_PLANNING=
CHAT_MODEL_MEMORY=

# ----- Anthropic (reserved; unused by default) -----
ANTHROPIC_BASE_URL=https://api.anthropic.com
ANTHROPIC_API_KEY=

# ----- App -----
# Runtime data is intentionally outside the source tree.
DATA_DIR=${HOME}/.local/share/weatherflow/data
DB_FILENAME=weatherflow.db
APP_HOST=127.0.0.1
APP_PORT=8765
LOG_LEVEL=INFO
CORS_ALLOWED_ORIGINS=http://127.0.0.1:3000,http://localhost:3000

# ----- Scheduler -----
SCHEDULER_ENABLED=true
EVENING_REFLECTION_CRON=22:00
WEEKLY_REVIEW_CRON=sun:21:00
SCHEDULER_TIMEZONE=local

# Bundled behavior sensors (same as ``wf sensors`` / POST /api/sensors/sweep).
# Runs on the machine hosting the backend; requires SCHEDULER_ENABLED=true.
SENSOR_SWEEP_ENABLED=false
SENSOR_SWEEP_CRON=09:00
# Comma-separated paths; empty = ~/Projects (git+workspace) and ~/Notes (notes) on server.
SENSOR_SWEEP_GIT_ROOTS=
SENSOR_SWEEP_NOTES_ROOTS=
SENSOR_SWEEP_WORKSPACE_ROOTS=

# ----- Dev Review providers -----
GITHUB_TOKEN=
GOOGLE_CALENDAR_ACCESS_TOKEN=
GOOGLE_CALENDAR_CALENDAR_ID=primary
GOOGLE_CALENDAR_BASE_URL=https://www.googleapis.com/calendar/v3

# ----- Frontend -----
NEXT_PUBLIC_API_BASE=http://127.0.0.1:8765

# ----- Hybrid memory -----
# Mid-term Markdown; default is DATA_DIR/memory if empty.
MEMORY_MARKDOWN_DIR=
# Long-term vectors: http://127.0.0.1:6333 when Qdrant is local; empty = SQLite fallback.
QDRANT_URL=
QDRANT_API_KEY=
QDRANT_COLLECTION=weatherflow_ltm
LTM_DEDUPE_THRESHOLD=0.92

# ----- CLI -----
WF_API_BASE=http://127.0.0.1:8765
# Browser URL for ``wf dashboard``; also used by ``wf stop`` to find the Next dev port.
WF_DASHBOARD_URL=http://127.0.0.1:3000
# If the CLI is used outside the repo clone, set absolute path to WeatherFlow root.
# WF_PROJECT_ROOT=

# ----- Docker Compose -----
# Host path mounted into the backend container at /app/data.
WF_DATA_DIR=${HOME}/.local/share/weatherflow/data
