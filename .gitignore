# Python
__pycache__/
*.py[cod]
*.so
build/
dist/
*.egg-info/
.venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# Local configuration and secrets
.env
.env.*
!.env.example
credentials.json
*_token.json

# WeatherFlow local state
.weatherflow/
.wf-data/

# Future desktop generated files
node_modules/
target/
*.tsbuildinfo

# Agent tooling
.worktrees/
.superpowers/
.codex/

# OS and editors
.DS_Store
.vscode/
.idea/
*.log

# Reproducible local release outputs (manifests remain tracked)
release/pyinstaller/
release/macos/*.app/
release/macos/*.dmg
release/.weatherflow-release.lock

# Local benchmark runs are generated by default. Curated evidence directories
# are force-added only after their source commit and manifest are verified.
eval/results/**
